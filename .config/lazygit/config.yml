os:
  open: "zed {{filename}}"
  edit: "zed {{filename}}"
  editAtLine: "zed {{filename}}:{{line}}"
  editAtLineAndWait: "zed {{filename}}:{{line}} --wait"
  openDirInEditor: "zed {{filename}}"

gui:
  theme:
    selectedLineBgColor:
      - reverse

git:
  log:
    showGraph: always
    showWholeGraph: true
